{
    "name": "Return Based Attribution",
    "description": "A web app for return-based analysis using Flask and React",
    "scripts": {
      "postdeploy": "flask db upgrade"
    },
    "env": {
      "FLASK_ENV": {
        "description": "The environment for the Flask app",
        "value": "production"
      },
      "REDIS_TLS_URL": {
        "description": "URL for Redis instance with TLS enabled",
        "required": true
      }
    },
    "addons": [
      "heroku-redis"
    ],
    "buildpacks": [
      {
        "url": "heroku/nodejs"
      },
      {
        "url": "heroku/python"
      }
    ]
  }
  